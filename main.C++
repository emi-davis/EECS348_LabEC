#include <iostream>
#include <string>
using namespace std;

string getInput() {
  string temp;
  cout << "Enter a string (or 'END' to quit): ";
  cin >> temp;
  return temp;
}
double extractNumeric(string user_input) {
  double result;
  return result;
}
int main() {
  string x = getInput();
  cout << x;
  return 0;
}